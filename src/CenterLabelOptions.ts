export default interface CenterLabelOptions {
    fontFamily: string
    text: string
    color: string
    minFontSize: number
    maxFontSize: number
    lineHeight: number
    sidePadding: number
}
